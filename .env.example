# Penciled.fyi Environment Variables
# Copy this file to .env.local and fill in your values

# ===========================================
# REQUIRED - Application will not work without these
# ===========================================

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Application URL (no trailing slash)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Google OAuth (for provider login)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google-calendar/callback

# Email Service (Resend)
RESEND_API_KEY=re_your_api_key
# Optional: Custom FROM address (default: penciled.fyi <noreply@penciled.fyi>)
# EMAIL_FROM=Your App <noreply@yourdomain.com>

# ===========================================
# PRODUCTION REQUIRED - Must set for production
# ===========================================

# Cron Job Authentication
# Generate with: openssl rand -hex 32
# CRITICAL: Without this, the /api/cron/reminders endpoint is publicly accessible
CRON_SECRET=

# Error Logging Webhook (Discord or Slack)
# Discord: Server Settings > Integrations > Webhooks > New Webhook > Copy URL
# Slack: Create App > Incoming Webhooks > Add > Copy URL
ERROR_WEBHOOK_URL=

# ===========================================
# OPTIONAL - Recommended for production
# ===========================================

# Upstash Redis for distributed rate limiting
# Without these, rate limiting uses in-memory store (single instance only)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# ===========================================
# NOTES
# ===========================================
#
# 1. Never commit .env.local to git
# 2. Set all PRODUCTION REQUIRED vars in Vercel dashboard
# 3. Rotate CRON_SECRET every 90 days
# 4. Keep a secure backup of all secrets
